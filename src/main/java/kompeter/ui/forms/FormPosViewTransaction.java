package kompeter.ui.forms;

import kompeter.ui.system.Form;
import kompeter.ui.utils.SystemForm;

@SystemForm(name = "View a Transaction", description = "View a specific transaction")
public class FormPosViewTransaction extends Form {
    private int currentSaleId;
}
